const { sendTelegramNotification } = require('./telegram-notifier');

// Тестовая функция для проверки отправки уведомлений
async function testTelegramNotification() {
  try {
    console.log('Отправка тестового уведомления в Telegram...');
    await sendTelegramNotification('🔔 Тестовое уведомление\n\nСистема уведомлений работает корректно!');
    console.log('Тестовое уведомление отправлено успешно!');
  } catch (error) {
    console.error('Ошибка при отправке тестового уведомления:', error);
  }
}

// Запуск теста
testTelegramNotification();